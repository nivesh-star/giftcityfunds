"""Static, non-executable content (FAQ copy, etc.) used by the page routes.

Kept separate from routes/ so a route file reads as "here's the logic that
decides what to show," not a wall of paragraph text to scroll past.
"""
